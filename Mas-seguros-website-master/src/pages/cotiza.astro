---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Cotiza tu seguri">
  <section class="space-y-6">
    <p class="text-sm uppercase tracking-[0.3em] text-emerald-400">Cotiza</p>
    <h1 class="title-heading text-4xl md:text-5xl">Cotiza tu seguri</h1>
    <p class="max-w-3xl text-base text-slate-600 md:text-lg">
      Inicia tu cotización con información básica para que podamos recomendarte la mejor
      cobertura posible.
    </p>
    <div class="grid gap-6 md:grid-cols-2">
      {["Tipo de seguro", "Cobertura deseada", "Datos de contacto", "Preferencias de comunicación"].map(
        (field) => (
          <article class="rounded-2xl border border-slate-200 bg-white p-6">
            <h2 class="title-heading text-xl text-slate-900">{field}</h2>
            <p class="mt-3 text-sm text-slate-600">
              Aquí se mostrará un paso del formulario para completar la cotización.
            </p>
          </article>
        )
      )}
    </div>
  </section>
</BaseLayout>
